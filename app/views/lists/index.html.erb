<h1><strong>All lists</strong></h1>
<% @lists.each do |list| %>
  <%= link_to list.name, list_path(list) %><br>
<% end %>

  <button type="button" class="btn btn-success"><%= link_to "Add a new list", new_list_path %></button>
